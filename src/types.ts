export interface FoodProps {
    id: number;
    name: string;
    price: number;
    description: string;
    available: boolean;
    image: string;
}

export interface newFoodProps {
    name: string;
    description: string;
    price: number;
    image: string;
}